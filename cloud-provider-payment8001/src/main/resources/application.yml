

server:
  port: 8001


spring:
  application:
    name: cloud-payment-service
  datasource:
#    type: com.alibaba.druid.pool.DruidDataSource    #当前数据源操作类型
    driver-class-name: org.gjt.mm.mysql.Driver      #mysql驱动包
    url: jdbc:mysql://127.0.0.1:3306/db2019?useUnicode=true&characterEncoding=utf-8&useSSL=false
# thinkpad-470
    username: ljj
    password: ljj
# redis
  redis:
    database: 0
    host: 192.168.43.111
    port: 6379
    password:
    jedis:
      pool:
        max-active: 200
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 2000ms
# dell
#    username: root
#    password: 123456

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.huayi.ljj.springcloud.dao
  config-location: classpath:config/mybatis.xml

## eureka 注册信息
eureka:
  client:
    # 表示是否将自己注册进EurekaServer 默认为true
    register-with-eureka: true
    # 是否从EurekaServer抓取已有的的注册信息，默认为true.单节点无所谓，集群必须设置为true才能配合ribbon使用负载均衡
    fetch-registry: true
    service-url:
      # 注册中心地址
#      defaultZone: http://localhost:7001/eureka/ # 单机版
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/ # 集群版
# LOG日志  需要时配置
#logging:
#  file:
#    path:
#  config: classpath:logback.xml


# threadPool config
threadpool:
  corePoolSize: 4 #  核心线程数
  maxPoolSize: 20 # 最大线程数
  queueCapacity: 20 # 队列最大长度
  keepAliveSeconds: 60 # 线程池维护线程所允许的空闲时间，默认为60s


#  file:
#    path: com.huayi.ljj.springcloud.dao
logging:
  level:
    com:
      huayi:
        ljj:
          springcloud:
            dao: debug
